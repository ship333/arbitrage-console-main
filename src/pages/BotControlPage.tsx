import { BotControl } from '@/features/bot-control';

const BotControlPage = () => {
  return (
    <div className="container mx-auto p-6">
      <h1 className="text-3xl font-bold mb-6">Bot Control</h1>
      <BotControl />
    </div>
  );
};

export default BotControlPage;
